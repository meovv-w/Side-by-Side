const api=require('../../utils/api');Page({data:{inviteCode:'',sharePath:'',records:[],rewards:[],progress:0},onShow(){api.listInvites().then(r=>{if(r.ok)this.setData({...r.data,records:r.data.records.map(item=>({...item,avatarText:(item.inviteeName||'同').slice(0,1)})),progress:Math.min(100,r.data.records.length/5*100)});});},copy(){wx.setClipboardData({data:this.data.inviteCode});},onShareAppMessage(){return{title:'和我一起加入同路行，自驾组队更省心',path:this.data.sharePath||'/pages/login/login'};}});
