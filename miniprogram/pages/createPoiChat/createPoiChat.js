const api=require('../../utils/api');Page({data:{form:{name:'',location:''}},input(e){this.setData({[`form.${e.currentTarget.dataset.key}`]:e.detail.value});},location(){wx.chooseLocation({success:l=>this.setData({'form.location':`${l.name} · ${l.address}`})});},submit(){const f=this.data.form;if(!f.name||!f.location)return wx.showToast({title:'请填写话题和地点',icon:'none'});api.createPoiChat(f).then(r=>{if(r.ok)wx.redirectTo({url:`/pages/poiChat/poiChat?id=${r.data._id}`});});}});
